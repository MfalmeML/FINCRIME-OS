[project]
name = "fincrime-os"
version = "0.0.1"
description = "Autonomous Real-Time Financial Crime Intelligence & Adaptive Decision Platform"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]